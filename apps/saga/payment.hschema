app payment

type Request = Integer
type Token = Integer
type Failure = Unit
type Empty = Unit

action release {
    input Token
    success Empty
    error Failure
    compensation none
}

action reserve {
    input Request
    success Token
    error Failure
    compensation release
}
